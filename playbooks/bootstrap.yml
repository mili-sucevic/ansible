---
- hosts: all
  tags: bootstrap
  vars:
     ansible_ssh_private_key_file: ../../../home/ansible/.ssh/id_rsa.pub

  become: true

  roles:
    - name: common

  post_tasks:
    - name: Reboot
      reboot:
      become: true
